---
# Machine-checkable rules. `ryo check` validates this block, `ryo classify`
# applies scope_overrides and forbidden_paths, and the SessionStart hook
# injects the whole file into every session. Delete any key you do not need.
version: 1
protected_branches:
  - main
  - release/*
required_reviewers:
  default: 1
# Paths agents must never modify directly. Changes here go through a human.
forbidden_paths: []
# Situations where the executor must stop and ask, in addition to the defaults
# (irreversible or destructive operations, security-sensitive actions, side
# effects outside the working branch, a plan so broken every path is a guess).
stop_conditions: []
# Path-based minimum scope. Anything touching these paths is classified at
# least this large, however small the diff looks.
scope_overrides: []
#  - paths: ["auth/**", "db/migrations/**"]
#    minimum_scope: feature
#    reason: "auth and schema changes always get design review"
evidence:
  review: required
  tests: required
audit:
  retain_ledgers: true
  retain_dir: .ryo/.state/audit
---

# Constitution

## Non-Negotiable Principles

These principles apply to all agents, skills, and workflows generated by ryo-kit. They cannot be overridden by individual agents or processes.

### Code Quality
- All code must pass linting and formatting checks before handoff
- Tests are required for new functionality

### Security
- Never commit secrets, credentials, or API keys
- Follow the principle of least privilege

### Process
- Human review is required before merging to production branches
- All changes must be traceable to a requirement or decision
- No completion claim without fresh verification evidence
- Every decision made on the user's behalf is recorded as a ruling in the ledger

---

*Edit this file to add your organization's non-negotiable principles. The prose is embedded into every generated agent and skill; the frontmatter is enforced by `ryo check`, `ryo classify`, and the session hook.*
